import { SearchInArrayPipe } from './search-array.pipe';

describe('SearchArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchInArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
